*.mp4 filter=lfs diff=lfs merge=lfs -text
# This backend-only fixture is unavailable in remote LFS storage. Pages does
# not need it to build the frontend, so keep its pointer as a regular file.
tests/fixtures/vision_v2/demo.mp4 -filter -diff -merge text
*.sh text eol=lf
