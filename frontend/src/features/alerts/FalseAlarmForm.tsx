import { X } from "lucide-react";
import { useState } from "react";
const reasons = ["Người thân đang tập thể dục", "Người thân đang nằm nghỉ", "Camera bị che", "Nhận diện nhầm", "Khác"];
export function FalseAlarmForm({ onCancel, onSubmit }: { onCancel: () => void; onSubmit: (reason: string, note: string) => void }) { const [reason, setReason] = useState(reasons[0]); const [note, setNote] = useState(""); return <div className="alert-modal-backdrop"><form className="alert-modal false-alarm-modal" onSubmit={(event) => { event.preventDefault(); onSubmit(reason, note); }}><button type="button" className="modal-close" onClick={onCancel}><X /></button><h3>Vì sao đây là cảnh báo sai?</h3><p>Phản hồi của bạn giúp cải thiện trải nghiệm nhận diện.</p><div className="reason-options">{reasons.map((item) => <label key={item}><input type="radio" name="reason" checked={reason === item} onChange={() => setReason(item)} />{item}</label>)}</div><textarea value={note} onChange={(event) => setNote(event.target.value)} placeholder="Thêm ghi chú (không bắt buộc)" /><div className="modal-actions"><button type="button" onClick={onCancel}>Quay lại</button><button className="confirm-safe" type="submit">Gửi phản hồi</button></div></form></div>; }

