import { History } from "lucide-react"; import PagePlaceholder from "./PagePlaceholder";
export default function HistoryPage(){ return <PagePlaceholder icon={History} title="Lịch sử" description="Theo dõi các sự kiện đã ghi nhận trước đây." />; }
