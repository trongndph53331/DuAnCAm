import { Settings } from "lucide-react"; import PagePlaceholder from "./PagePlaceholder";
export default function SettingsPage(){ return <PagePlaceholder icon={Settings} title="Cài đặt" description="Thiết lập hệ thống và tùy chọn sử dụng." />; }
